// UI-only gate to prevent accidental clicks, not real security (see ImportExcelTab's
// SAVE_WORKER_URL — actual auth happens server-side via X-App-Key).
export const ADMIN_PASSWORD = 'Ssj4900036!@#'
